// //creating an array
// let state =["sitapur","kanpur","delhi",]

// //accessing an array
// console.log(state);
// console.log(state[1]);

// //length of array
// length=state.length
// console.log("length of the array is:",length);
// console.log(state.length);

//array methods
let num=[20,60,80,25]
//push()->to insert elemente in the last
num.push(10)
console.log(num);

// unshift()->to insert elemente in the the first
 num.unshift(1)
console.log(num);

//pop()->to delete elemente in the last
num.pop()
console.log(num);

//shift()->to delete element in the first
num.shift()
console.log(num);
